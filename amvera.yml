meta:
  environment: csharp
  toolchain:
    name: dotnet
    version: "10.0"
build:
  image: null
  buildCommand: dotnet publish -c Release -o /app/publish
  artifacts: /app/publish/*
run:
  image: null
  buildFileName: bin/MyCompany 
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
